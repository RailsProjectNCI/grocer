<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <%= favicon_link_tag(source='basket.png') %>
    <link href="https://fonts.googleapis.com/css?family=Nunito" rel="stylesheet">
  </head>
  <body>
    <nav class="navbar navbar-inverse">
        <div class="container-fluid">
          <div class="navbar-header">
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>                        
              </button>
              <%= image_tag("basket.png", :alt => "rss feed", :style => "height:60px")%>
          </div>
          <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav navbar-left">
              <li>
                <%= link_to " Register", new_user_registration_path, class:"glyphicon glyphicon-user white bold"%>
              </li>
              <% if user_signed_in? %>
                <li><%= link_to " Log Out", destroy_user_session_path, method: :delete, class:"glyphicon glyphicon-log-out white bold"%></li>
                <li><%= link_to " My Account", user_path(current_user.id), class:"glyphicon glyphicon-pencil white bold" %></li>
              <% else %>
                <li> <%= link_to " Log In", new_user_session_path, class:"glyphicon glyphicon-log-in white bold"%> </li>
              <% end %>
              </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><%= link_to "Home", root_path, class:"white bold" %></li>
                <li><%= link_to "About", pages_about_path, class:"white bold" %></li>
                <li><%= link_to "Products", products_path, class:"white bold"  %></li>
                <li><%= link_to "Delivery", pages_delivery_path, class:"white bold"  %></li>
                <li class="search">
                  <%= form_tag(products_path, :method => "get", id: "search-form") do %>
                  <%= text_field_tag :search, params[:search], placeholder: "Search products" %>
                  <%= submit_tag "Search"%>
                <% end %>
                </li>
              </ul>
          </div>
        </div>
    </nav>
  	<div class="container">
  		<p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
    	<%= yield %>
    </div>
  </body>
</html>
